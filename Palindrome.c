int main()
int num,rev num,rev num,rem,temp num;
printf("enter the integer");
scanf("%d",&num);
temp num=num
while(temp num!=0)
{
rem=temp num%10
rev num=rev num*10+rem;
temp num=10
if(rev num=num)
printf("palindrome")
else
printf("not palindrome")
return 0;
}
